name = "tokleperlen-assistant"
main = "src/index.js"
compatibility_date = "2024-11-11"

# --- Secrets (DO NOT put values here). Set them with wrangler after deploy: ---
#   npx wrangler secret put ANTHROPIC_API_KEY      # from console.anthropic.com
#   npx wrangler secret put ASSISTANT_PASSPHRASE   # the code you give Siri
#
# --- Optional: switch model without touching code by uncommenting below ---
# [vars]
# MODEL = "claude-sonnet-4-6"   # or claude-opus-4-8 / claude-haiku-4-5
